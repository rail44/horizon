# イベント・結果・画面の寿命の整理（2026-09-25）

対象は main `5ce74c00` からの non-board コード。前巡の持越し3件を実装した。

| 対象 | 変更と効果 |
| --- | --- |
| `ProviderEvent` | 永続イベントと4種類の一時通知をenumで区別。保存・実行・配信・表示からダミーイベントと排他的なOptionを撤去。 |
| `ToolCallResult` | 成功・失敗・拒否・取消し・再試行による置換をenum化。表示と承認記録は本文の文字列に依存せず、providerへの結果も実行時／再生時とも `{outcome, output}` に統一。 |
| モーダル | リスト・対象・購読・応答待機タスクを1つの所有型へ集約。閉じ直すと旧応答を破棄し、別段階への応答で選択を動かさない。確定／取消し時の既存フォーカス規則は保持。 |

追跡中に見つかった問題も修正した。取消しを成功件数に数えず、未実行の再試行を取消しとして閉じる。
置換された実行の遅延結果はproviderの応答に採用しない。失敗・拒否・取消しによる `memory.update` は
記憶更新済みと扱わない。旧DBはシーケンス番号が一致していても新形式のログと同一ではないため、
実際に再構築時に記録する形式番号でキャッシュを無効化する。旧データを通常処理で変換する分岐は置かない。

**agent wire v24・event-log v3**。v1/v2からの変換器は原本を変更せず、曖昧なセッションを理由付きで
別アーカイブへ残す。承認内の過去の結果、後続要求を指す再試行も変換する。
[形式・移行・再起動手順](../agent-history-format.md)を参照。実データの変換と稼働アプリの再起動は未実施。
boardの機能は対象外。共有ログを生成するboardテストの版数1行だけを追従させた。

## 追加調査で維持したもの

non-boardの全体走査は357ファイル・2,425関数、board固有の33構文範囲を除外。
`target/refactor-audit/5ce74c000ce2-20260924T190015Z-198b5dbc/` に入力・解析結果を保存した。
途中の同様の走査に構造検索を重ねた入口は、複数bool/Optionの構造体45件、種類を文字列で分岐するmatch19件。
混在ファイル内のboard分岐は判定対象から除いた。型の数や複雑度の減少を採用理由にはしていない。

- 端末装飾、modifier、sandbox権限、子処理の失敗と上限到達、検索の2種類の打切りは同時に成立する。単一enumにしない。
- bashの終了と出力回収、通信障害の種類とストリーム途中かどうかも別の情報。無理に全組合せを列挙しない。
- ツールIDは実行入口で既存の型に解決する。未知のツールも描画する表示側や、未知の種類を保持する制御APIのデコードには文字列境界を残す。
- runtime reloadは既存の実行中ガードで直列化され、復元・再接続にはruntime照合がある。共通traitを追加する利益は確認できず、端末とagentの異なる破棄契約を維持する。

これは全型の設計を精査し尽くしたという意味ではない。レビュー台帳は今回追跡した4箇所だけ更新した。

## 検証

- Python変換テスト12件。Rust側でも変換結果を実デコーダ・DuckDB・表示へ流し、5種類の結果、原本保存、曖昧データの隔離を確認。
- sandboxed workspaceテスト2,109件。通知の保存除外、取消しと拒否、遅延結果、記憶更新、旧DBの再構築、画面を閉じ直す際の応答・購読破棄を検証。
- 通常profileのworkspaceテスト2,260件成功・15skip。実ソケット、HTTP/SSE、daemon再起動と履歴再生も通過。
- 実WASMの描画・theme反映・reloadと、paneの読込／retarget資源解放の2テストが成功。
- workspaceビルド、fmt、Clippy、wire schema、WASM targetの必須ゲートを通過。
- 隔離した実UIの再起動で、2タブ・2分割・同じ3端末sessionを復元。
