/target/
# The preview plugin is its own sub-workspace, so it has its own target/.
/preview-plugin/target/
/.claude/settings.local.json
/.claude/worktrees/
/.claude/review-queue/
/.horizon/*
!/.horizon/skills/
/.envrc
/.envrc.local

# Playwright MCP page-snapshot debris (session tooling output, never project content)
.playwright-mcp/
